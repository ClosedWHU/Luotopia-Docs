---
title: 车辆 / 开锁 / 还车 / 车辆控制接口
sidebar_label: 车辆与开锁还车
description: 扫码车辆详情、锁与头盔盒指令、订单创建与轮询、临时锁车、救援、拍照还车与图片上传接口。
sidebar_position: 2
---

## 5.2 车辆 / 开锁 / 还车 / 车辆控制（重点）

| 接口路径 | 方法 | 关键参数 | 用途 | 调用处 |
|---|---|---|---|---|
| `/miniMango/v1/ebike/stock/number` | GET | query: `number`(车牌/扫码值), `isScanQR`(bool) | **扫码取车详情**（key `getStock`）。返回 `stock{region,battery{mileage},price{timeUnit,mileageUnit,floorCost,baseTimeUnit},priceTrans{...},discountRateTrans,outsideRegion,isWuhanDaXue,location{lngLat}}`、`box{hasHelmetLock,helmetOptions{helmetIsLost,userCanOpenHelmetLock}}`、`charteredEbikeCard{hasCard,config[]}`、`replaceRegion`(跨区域时替换全局运营配置)、`needPayDeposit`+`message`、`customPriceNotice`；`state===6` 表示车辆报失 → 弹 `findLostTraction`（含 `stockInLostAlertContent` 与 `findLostAward` 悬赏金额） | `pages/readyUnlock/readyUnlock.js:406` |
| `/miniMango/v1/ebike/stock/number` | GET | query: `number`(markerId) | 地图车辆 marker 点击查详情（key `stockInfo`，同路径不同 key） | `pages/index/index.js:1179` |
| `/miniMango/v1/ebike/stock/number/getInfo` | GET | query: `number`(车牌，前端先 `trim()`) | 反馈页把车牌解析成 `stockId` | `packages/business/pages/feedbackEntry/feedbackEntry.js:206` |
| `/miniMango/v1/ebike/stock/number/vaildCanUse` | GET | query: `number`(=`globalData.scanCode`) | 交押金前校验「该车是否对本用户开放」（武大校内车限制），返回 `{check:bool}`。注意路径里 `vaild` 是服务端拼写错误 | `pages/deposit/deposit.js:551` |
| `/miniMango/v1/ebike/stock/number/beforeScan` | — | — | 扫码前免押判断（key `isFreeDeposit`）—— **仅定义，未调用**；免押态实际取自 `getRegion`/`getUserInfo` 的 `isFreeDeposit` 字段 | `utils/util.js` |
| `/miniMango/v1/ebike/stock/near` | — | — | 附近车辆（key `nearStock`）—— **仅定义，未调用**；首页车辆点位走 `parkingLot/near` 的 `stocks[]` | `utils/util.js` |
| `/miniMango/v1/ebike/stock/help` | GET | query: `carNo`(扫码车牌) | 「用户摆车」任务：返回 `{alarmInfo{stock,order},preOrderResult{orderId}}`，判定该车是否需要挪动 | `pages/stockHelpJob/stockHelpJob.js:34` |
| `/miniMango/v1/ebike/stock/help/job` | POST | body: `beforeImg`, `afterImg`(CDN URL), `stock`(车辆ID) | 提交摆车任务，返回 `{state:"success",msg}` | `pages/stockHelpJob/stockHelpJob.js:74` |
| `/miniMango/v1/ebike/box/{id}` | GET | path: `id`(=`order.box`) | 取锁/头盔盒详情：`hasHelmetLock`、`helmetOptions.helmetIsLost`、`helmetOptions.userCanOpenHelmetLock` → 决定 `hashelmetLock` | `pages/index/index.js:534` |
| `/miniMango/v1/ebike/box/{id}/command` | PUT | path: `id`(boxId)；body: `command`, `param:{}`, `dataSource:6` | **直接向锁/头盔盒下发指令**。已见两种 `command`：<br/>• `command:17` → 手动开锁（`pages/index/index.js:2571` `unlock()`，成功 toast「开锁成功」）<br/>• `command:6` → 查询头盔在位状态，读 `data.stockBooleanState.helmetStay` / `status.helmetReact`，未归还则 toast「头盔未归还」并阻断临时锁车（`pages/index/index.js:1314` `checkHasHelmetLock`）<br/>`dataSource:6` 推测为指令来源枚举（小程序端） | `pages/index/index.js:1314,2571` |
| `/miniMango/v1/order/order` | POST | body: `num`(车牌号) | **扫码开锁 / 创建订单**。返回 `{order{_id,...},outsideRescue{...}}`；若同时返回 `outsideRescue` 则写入 `globalData.pendingOutsideRescue`（区外临时通电待生效）。成功后 `event.emit("create_order_end")` + `emit("un_lock")` + `navigateBack({delta:2})` 回首页进入骑行态 | `pages/readyUnlock/readyUnlock.js:333` |
| `/miniMango/v1/order/order/processing` | GET | 无 | **进行中订单轮询**（首页 `setInterval(fetchCurrentOrder, 10000)`）。返回 `{order{_id,box,stockNo,route{end{lngLat},distance},lease{totalDuration,totalDurationByLock},payInfo{rent{finalTotal},dispatchCost,dispatchCouponCost,newUserReductionCost,parkingAreaReturnDiscount,isMergedDispatchCost,totalAmount},lock{isLocked},charteredEbikeCard,state,userRealTarget,ignoreHelmet},stock{battery{mileage}},newParkingInfo{info{bubbleContent,alertTitle,alertContent,dispatch,rescue{active,available,expiresAt}}},discountParkingLotInfo,finishOrderDisplay,forceReturnOrder}` | `pages/index/index.js:480`（`fetchCurrentOrder` 无回调参数时） |
| `/miniMango/v1/order/order/snap` | PUT | 无 | **还车前判定快照**（key `finishGetCurrentorder`，同一调用点按有无 callback 二选一）。返回与 `processing` 同构 + `allowForcedReturn`/`allowForcedReturnReason`、`allowPhotoAppeal`/`allowPhotoAppealReason`、`parkingPrompt`、`orderFinished`、`newParkingInfo.info.bubbleContent`（含「不在停车区」「停车区外」「服务区外」等关键字，前端用 `indexOf` 匹配来决定弹窗文案与是否收调度费） | `pages/index/index.js:480`（`fetchCurrentOrder(cb)` 有回调时） |
| `/miniMango/v1/order/order/unLock` | PUT | 无 | **行程中重新开锁**（临时锁车后点击「开锁」→ 二次确认弹窗「确认开锁吗?」） | `pages/index/index.js:1240` |
| `/miniMango/v1/order/order/lockWithExpires` | PUT | body: `expires:40` | **临时锁车**。调用前先 `checkHasHelmetLock()`（有头盔锁则先发 `boxCmd command:6` 确认头盔已归还）。`expires:40` 为硬编码常量（单位由服务端定义，推测为分钟数/超时窗口）；成功后 `isLock=true`。若当前处于区外救援态（`rescue.active || rescue.available`）则前端直接拦截并 toast「车辆救援中不能临时锁车」 | `pages/index/index.js:1198` |
| `/miniMango/v1/order/order/finish` | PUT | body（可选组合）：`{}` / `{forceReturn:true}` / `{skipParkingRecognition:true, appealPhotos:[cdnUrl]}` / `{payOutRegionReturn:true}` | **还车/结束行程**。前置：`refreshWxLocation()` 刷新定位 → `showLoading("行程结束中")`。<br/>• 若开启拍照还车（`region.enablePhotograph && !enablePhotographClose` 或 `operationPattern===1 && !enablePhotographClose`）且已上传照片，先 `takePhotoFinishOrder` 再 `finish`<br/>• 失败分支按服务端返回的 `allowForcedReturn` → 弹「多次还车失败，是否强制还车」→ `forceReturn:true` 重试；`allowPhotoAppeal` → 弹「停车识别未通过…拍照结束」→ 走 `startPhotoAppeal()`；`parkingPrompt` → 弹 AI 识别提示（「需要将车停放在马路牙子上」）；`orderFinished` → 静默关弹窗<br/>• 成功后按 `payInfo.dispatchCost` 判定「是否在停车区」，展示结算面板；`state===8` 表示有未付金额 → `hasUnpayOrder=true`；`depositUpgrade` 为真则跳押金购卡 H5（URL 带 `token`）；`forceReturnOrder` 或 `parkingAreaReturnDiscount>0` 则走「强制还车/停车区返还优惠」抽屉 | `pages/index/index.js:1406,1506`；`pages/lockBike/lockBike.js:23`（还车失败页直接重试 `finish`，无 body） |
| `/miniMango/v1/order/order/takePhotoFinishOrder` | PUT | body: `orderId`, `photos:[cdnUrl,...]` | 拍照还车：提交停放照片（先经 `/oss/dispatchWorkOrder` 上传拿 `cdnUrl`），成功后紧接着调 `finish` | `pages/index/index.js:1394` |
| `/miniMango/v1/order/order/outsideRescue` | POST | body: `{}` | **服务区外临时通电救援**。返回 `{expiresAt, remainingSeconds, distance, durationSeconds, nearestFencePoint:[lat,lng]}` → 前端启动倒计时、在地图上画「车 → 最近围栏点」蓝色箭头线、弹窗「已为你临时通电，请在 N 分钟内骑回服务区」；剩余 `<1` 分钟 toast 提醒；到期后自动 `fetchCurrentOrder()`。恢复断电时弹「车辆已恢复断电 / 你还有一次临时启动机会」。救援期间禁止临时锁车 | `pages/index/index.js:658` |
| `/miniMango/v1/order/order/{id}/reasonForOutsideParkingLot` | PUT | path: `id`(orderId)；body: `reasonForOutsideParkingLot`(原因索引，来自 `e.detail.index`) | 停车区外还车原因上报 | `pages/index/index.js:1647` |
| `/miniMango/v1/order/order/{id}/badOrderReason` | PUT | path: `id`(orderId)；body: `reason`(0–3), `description`(仅 `reason===3` 时带) | 「问题行程」原因反馈（`MGBadOrderModal` 组件收集） | `pages/index/index.js:1734` |
| `/miniMango/v1/order/order/parkingLotDiscount` | POST | body: `parkingLotId`, `target:[lng,lat]` | **设置优惠停车区目标**：骑行中选定目的地后绑定某个停车点，还到该点享 `parkingAreaReturnDiscount`。成功 `emit("confirmParkingLotDiscount")` → 首页记录 `ParkingLotDiscountTarget` + `userRealTarget` 并规划导航 | `packages/business/pages/recommendedParkingLot/recommendedParkingLot.js:265` |
| `/miniMango/v1/order/order/reservation` | POST | body: `stock`(车辆 `_id`) | **预约车辆**（首页 30s 轮询 + 预约按钮） | `pages/index/index.js:2625` |
| `/miniMango/v1/order/order/reservation` | GET | 无 | 查询当前预约：`{reservation{stock},stock{number{custom},location{lngLat}}}` → 显示「您已有预约中的车辆,车牌号为X」并画线 | `pages/index/index.js:2691` |
| `/miniMango/v1/order/order/reservation` | DELETE | 无 | 取消预约 | `pages/index/index.js:2663` |
| `/miniMango/v1/order/order/userComment` | POST | body: `orderId`, `point`(1–5 星), `commentLabel:[标签]` | 行程评价。`orderComponents/comment/comment.js` 里 `orderId` 兜底为字面量 `"12345678"`（测试残留） | `components/MGTripSettlePanel/MGTripSettlePanel.js:167`；`orderComponents/comment/comment.js:66` |
| `/miniMango/v1/order/order/cancelHelmet` | POST | body: `{}` | **临时关闭头盔检测**：弹窗声明「30天内仅允许两次」，成功后 `ignoreHelmetCheck=true`、`hashelmetLock=false`；失败 toast `HCHE1 - 关闭失败` | `pages/index/index.js:2926` |
| `/miniMango/v1/order/order/ErrorInfo` | — | — | 订单错误信息（key `errorInfo`）—— **仅定义，未调用** | `utils/util.js` |
| `/miniMango/v1/order/order/{id}` | GET | path: `id` | 行程/订单详情（结算面板刷新、行程详情页、反馈页订单卡片） | `pages/journeyDetail/journeyDetail.js:18`；`pages/index/index.js:1682`（`refreshSettleOrder`，带 `other:{silent:true}`）；`packages/business/pages/feedbackEntry/feedbackEntry.js:181` |
| `/miniMango/v1/order/order` | GET | query: `lastId` | 行程列表（游标分页），元素含 `route.distance`、`lease{totalDuration,totalDurationByLock,startTime,endTime}`、`payInfo{totalAmount,dispatchCost}`、`state`、`isCommutingCardOrder` | `pages/journey/journey.js:52`；`packages/business/pages/orderSelectList/orderSelectList.js:60` |
| `/miniMango/v1/record/illegalPark/valid` | GET | 无 | 违停停用校验：返回 `{endTime}` 则弹「因您上次未规范停车，已被限制停用三天。限制时间至：X」并阻断开锁 | `pages/readyUnlock/readyUnlock.js:191`；`pages/index/index.js:2095` |
| `/oss/dispatchWorkOrder` | POST multipart | 表单字段恒为 `file`（调用方传的 `name` 被忽略：`parkingAppeal.png`/`dispatch.png`/`p{index}.png`） | **通用图片上传**，返回字符串 JSON `{cdnUrl}`。用于：还车拍照、拍照申诉、还车失败页拍照、摆车前后照片 | `pages/index/index.js:1354,2419,2451`；`pages/lockBike/lockBike.js:70`；`pages/stockHelpJob/stockHelpJob.js:128` |
| `/oss/reportedAbuse` | POST multipart | 同上 | 问题反馈图片上传（`mg-photo-grid` 组件的默认 `uploadUrl`） | `packages/business/components/mg-photo-grid/mg-photo-grid.js:51`；`packages/business/pages/feedbackEntry/feedbackEntry.js`（`data.uploadUrl = "reportedAbuse"`） |
